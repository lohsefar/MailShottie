# SMTP Configuration (Required)
# SMTP server hostname (required)
# Example: mail.yourhost.com, smtp.gmail.com, smtp.sendgrid.net
SMTP_HOST=mail.yourhost.com

# SMTP port number (optional, defaults to SMTP_DEFAULT_PORT or 587)
# Port 587 (recommended): SMTP submission with STARTTLS - best for deliverability
# Port 465: SMTP over SSL/TLS (SMTPS) - also secure but less commonly used
SMTP_PORT=587

# Default SMTP port if SMTP_PORT is not set (optional, defaults to 587)
SMTP_DEFAULT_PORT=587

# Port number that triggers secure connection (optional, defaults to 465)
# If SMTP_PORT matches this, secure will be automatically set to true
SMTP_SECURE_PORT=465

# Force secure connection (optional, auto-detected based on port)
# Set to "true" for implicit SSL/TLS (required for port 465)
# Set to "false" for STARTTLS (default for port 587)
# If not set, automatically true for SMTP_SECURE_PORT, false for other ports
SMTP_SECURE=false

# SMTP authentication username (required)
# Usually your email address or SMTP username provided by your email service
SMTP_USER=your-name@yourdomain.com

# SMTP authentication password (required)
# Can use either SMTP_PASS or SMTP_PASSWORD - both are supported
SMTP_PASSWORD=your_password_here

# Default "From" address for emails (optional)
# Format: "Display Name <email@domain.com>" or just "email@domain.com"
# Note: This may be overridden by the from field in individual email sends
SMTP_FROM="Your Name <your-name@yourdomain.com>"

# Sender Configuration (Optional)
# Domain name for generating random sender addresses
# Used when "Use Random Sender Addresses" is enabled
# needs to be allowed to send as *@yourdomain.com
SENDER_DOMAIN=yourdomain.com

# Default sender email address when random senders are disabled (optional, defaults to loadtest@sauerdev.com)
# This email will be used as the "from" address for all emails when random senders are not enabled
DEFAULT_SENDER_EMAIL=your-name@yourdomain.com